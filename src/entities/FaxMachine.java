// Defines the package.
package entities;

/**
 * The FaxMachine class is used to store faxes, which are single orders.
 */
public class FaxMachine {
    // Defines the contructor methods.
    /**
     * The main constructor for a FaxMachine.
     *
     * @param server the Server that the fax machine belongs to.
     */
    public FaxMachine(Server server) {
        // Don't worry about this.
    }

    // Defines the functional methods.
    /**
     * The addOrder method adds an order to the FaxMachines current orders, as of now, it also
     * updates the server with the order and never even adds it to the orders property of a fax
     * machine.
     *
     * @param order the Order to be added to the FaxMachine.
     */
    public void addOrder(Order order) {
        // Don't worry about this.
    }
}
